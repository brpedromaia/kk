#!/bin/bash

if [ ! -z "$KK_AUTO_CREATION_FILE" ];then
  AUTOD_CLUSTER_NAME=`yq ".cluster.name" $KK_AUTO_CREATION_FILE`
  echo "
    KK_CONTEXT=kind-$AUTOD_CLUSTER_NAME
    KK_NAMESPACE=default
  " > $HOME/.kkrc
  kk autocreate
fi

$@